setup: ## Install dependencies
	python3.8 -m venv venv
	./venv/bin/python -m pip install pip==20.3
	./venv/bin/python -m pip install -r requirements.txt

run: ## Run the app with no reload
	source run_venv.sh